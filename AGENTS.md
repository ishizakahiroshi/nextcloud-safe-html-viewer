# Agent Entry Point (nextcloud-safe-html-viewer)

This repository's operational guidance is maintained in `CLAUDE.md`.

- Project overview, conventions & docs/plan rules: `./CLAUDE.md`
- Public-facing docs: `./README.md`, `./SECURITY.md`, `./CHANGELOG.md`, `./docs/`
- Internal planning / research notes (private, not committed): `./docs/local/`
- Local/private additions (if present, not committed): `./CLAUDE.local.md`
- Tool-specific local notes (if present, not committed): `./AGENTS.local.md`

This is a **public** repository carved out of a private codebase. Never commit
customer names, real domains, server addresses, credentials, file IDs, or
operational logs. Run the "公開前チェックリスト" in `CLAUDE.md` before committing.

Personal/global AI rules are intentionally kept outside this repository. Use each
AI tool's supported global instruction location for user-specific rules; this file
must remain valid for a fresh public clone with no private files.

If any project guidance conflicts, follow `CLAUDE.md`.
